<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .user-img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div>
        <%for(user of users){%>
            <a href="/user/<%=user._id%>"><span>
                <span><img src="<%=user.profile%>" class="user-img" alt=""></span>
                <span><%=user._id%></span>
                <span><%=user.name%></span>
                <span><%=user.username%></span>
            </span></a>
            <br>
        <%}%>
    </div>
</body>
</html>