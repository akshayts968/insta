<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Information</title>
    <style>
        .user-img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <h2>User Information</h2>
    <ul>
        <li>Profile Image: <img src="<%= user.profile %>" class="user-img" alt="Profile Image"><a href="/user/<%=user._id%>/change">delete</a></li>
        <li>Username: <%= user.username %></li>
        <li>Name: <%= user.name %></li>
        <li>Field: <%= user.field %></li>
        <li>Number of Posts: <%= user.nPost %></li>
        <li>Number of Followers: <%= user.nFollowers %></li>
        <li>Number of Following: <%= user.nFollowing %></li>
        <a href="/user/<%=user._id%>/newpost">NewPost</a>
    </ul>
    <% for(post of user.post){%>
    <div class="img">
        <img src="<%=post.videourl%>" alt="" user-img>
        <form method="post" action="/user/<%=user._id%>/delete/<%=post._id%>?_method=DELETE"><button>delete</button></form>
    </div>
    <%}%>

</body>
</html>
